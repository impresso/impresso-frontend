version: '3.7'
services:
  web:
    image: nginx:latest
    ports:
      - '8080:80'
    volumes:
      - ./dist:/usr/share/nginx/html
      - ./nginx.conf:/etc/nginx/nginx.conf
    command: nginx -g 'daemon off;'
